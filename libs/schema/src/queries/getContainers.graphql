query GetContainer {
  containers {
    id
    content {
      declaredType
      correctedType
      probability
    }
    boat {
      id
      name
    }
    hasFood
    hasMedical
    timeToLoad
  }
}
